version: 2.1
jobs:
  requirements:
    docker:
      - image: 'cimg/base:2020.12'
    steps:
      - run:
          command: gem install bundler -v 2.0.1
  build:
    docker:
      - image: 'cimg/base:2020.12'
    steps:
      - run:
          command: bundle install
  test:
    docker:
      - image: 'cimg/base:2020.12'
    steps:
      - run:
          command: 'rake ci:all'
workflows:
  version: 2
  build-and-test:
    jobs:
      - requirements
      - build:
          requires:
            - requirements
      - test:
          requires:
            - build
